$("#order-item-<%= @item.id %>").remove();
$('.cart-total').text("<%= cart_items %>");
$('.cart-price').text("<%= number_to_currency current_order.total_price %>");
<% if cart_items == 0 %>
  $('#finalize-link').remove();
<% end %>
